# 第 2 阶段验证记录

> 验证日期：2026-09-26

## 自动验证

| 项目 | 结果 |
|---|---|
| Release 构建 | 通过，0 警告、0 错误 |
| 领域测试 | 9 项通过；覆盖完整性、自审限制、乐观并发、多场景定义隔离及定义来源关联 |
| 应用测试 | 3 项通过；覆盖词义隔离查询与 CSV 特殊字符解析 |
| HTTP 测试 | 5 项通过；覆盖健康检查、系统信息、查询、第二阶段 OpenAPI 路由及管理端静态资源 |
| 管理端脚本测试 | 2 项通过；覆盖多语言、多义项、多场景审核差异与首次发布基线 |
| 浏览器页面检查 | 已检查桌面与移动端布局、无水平溢出；已验证术语语言切换与新增术语行 |
| EF Core 迁移生成 | 已生成 `Stage2TerminologyWorkflow` |

## 待环境验证

- 使用真实 PostgreSQL 执行迁移 Up/Down 演练。
- 接入真实 OIDC 身份中心后验证五类角色权限矩阵。
- 使用已确认许可证的真实术语文件做 100～5000 行导入压测。
- 发布 Outbox 消费者接入后验证索引最终一致性。

## 已知边界

- CSV 是本阶段首个可用批量格式；XLSX、TBX 解析器待后续接入。
- 管理端静态页面已接入宿主，真实身份服务和 PostgreSQL 数据环境仍需联调。
- `revisionToken` 冲突使用业务错误码 `TriDict:RevisionConflict`，宿主已统一映射为 HTTP 409。
